[project]
name = "that-movie-night-life"
version = "0.1.0"
description = "Data refresh scripts for the That Movie Night Life Apple app."
requires-python = ">=3.12"
dependencies = [
    "letterboxdpy>=6.5.2",
    "playwright>=1.58.0",
]
